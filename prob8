#include <iostream>
#include <conio.h>
using namespace std; 

int main ()
{
	int a;
		cout << "Enter A Number: ";
		cin >> a;
		
		cout << "\nMULTIPLICATION TABLE" ;
		
	for (int i = 1; i<=10; i++)
	{
		cout << "\n" << a << "x " << i << " = " << (a*i);
	}
		 	
	_getch ();
	return 0;
}
